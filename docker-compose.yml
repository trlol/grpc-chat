version: '3.8'

services:
  grpc-chat-server:
    build: .
    container_name: grpc-chat-server
    ports:
      - "50051:50051"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped